# 테스트빌드 전용 폴더

이 폴더는 테스트빌드와 임시 검증 파일만 둡니다.

- 테스트용 파일은 제목과 파일명에 `테스트빌드` 또는 `test-build`처럼 명확한 명사를 넣습니다.
- `demo`는 테스트용 명칭으로 쓰지 않습니다. 데모는 레퍼런스/시각 기준/비교용 파일입니다.
- 본편 파일, 레퍼런스 파일, 데모 파일을 삭제할 때는 이 폴더의 테스트빌드와 혼동하지 않도록 정확한 경로를 확인합니다.
- 임시 테스트빌드는 목적이 끝나면 정확한 경로를 확인한 뒤 삭제합니다.

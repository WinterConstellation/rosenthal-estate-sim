# 작업 규칙

- 작업 후 `docs/work_log.txt`에 KST 날짜/시간, 작업 요약, 수정 파일을 기록한다.
- 작업 기록은 같은 커밋에 포함하고, 커밋 SHA는 최종 보고에 포함한다.
- 작업 흐름/인계 메모는 `docs/pingpong.txt`에 최신 상태로 정리한다.
- 삭제 전에는 정확한 경로와 용도를 확인한다.
- `reference/`는 보존/비교용, `test-builds/`는 테스트빌드 전용이다.
- `demo`는 테스트용 명칭으로 쓰지 않는다.